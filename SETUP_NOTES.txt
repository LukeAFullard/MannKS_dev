Environment Setup Notes

1. System dependencies installed: r-base, libcurl4-openssl-dev, libtirpc-dev.
2. R packages installed one by one: plyr, tidyr, lubridate, gam, NADA, ggplot2, viridis, ggpubr.
   - Note: ggpubr required manual installation of dependencies (car, rstatix) to avoid timeout.
   - Icens installed from Bioconductor.
3. Python environment created in `venv` and dependencies installed from `dev-requirements.txt`.
4. Verification:
   - Tests passed (153 passed).
   - Validation script 01_Simple_Trend ran successfully.
